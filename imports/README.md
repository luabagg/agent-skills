# Skill imports

Pin external skill sources here for Agentfolio `skills.imports` (plan/verify in v1).

Existing curated sources still live in `../curated-skills.json` and are installed via the legacy `agent-skills install curated` flow until Agentfolio applies import pins.
